package com.ascend.admin.dto;

/**
 * Enum representing the types of moderation actions an admin can take on a user.
 */
public enum ModerationAction {
    WARN,
    SUSPEND,
    BAN,
    LEADERBOARD_RESTRICT,
    UNFLAG
}
