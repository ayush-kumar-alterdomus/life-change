export interface ArcDefinition {
  readonly id: string;
  readonly name: string;
  readonly description: string;
  readonly themeColor: string;
  readonly sampleQuests: string[];
  readonly icon: string;
}
