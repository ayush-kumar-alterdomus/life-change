export interface OnboardingPayload {
  readonly selectedGoals: string[];
  readonly difficulty: string;
  readonly personalityType: string;
  readonly selectedArc: string;
  readonly selectedAvatar: string;
}
